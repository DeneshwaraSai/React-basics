import React, { Component } from "react";

export class FragmentDemo extends Component {
  render() {
    return (
      <React.Fragment>
        <h1> Fragment Demo </h1>
        <p> 
          Web pages can be created and modified by using professional HTML
          editors. However, for learning HTML we recommend a simple text editor
          like Notepad (PC) or TextEdit (Mac).{" "}
        </p>
      </React.Fragment>
    );
  }
}

export default FragmentDemo;
